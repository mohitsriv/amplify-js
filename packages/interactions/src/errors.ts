const errors = {
    sendMessage39AWSLexProvider: "Bot + botname + does not exist",
    sendMessage42AWSLexProvider: "No credentials",
    sendMessage92AWSLexProvider: "Not implemented yet",
    onComplete99AWSLexProvider: "Bot + botname + does not exist",
    addPluggable83: "Bot + pluggable.getProviderName() + already plugged",
    send90: "Bot + botname + does not exist",
    send96: "Bot + botProvider + does not have valid pluggin did you try addPluggable first?",
    onComplete105: "Bot + botname + does not exist",
    onComplete110: "'Bot + botProvider + does not have valid pluggin did you try addPluggable first?'"
};
